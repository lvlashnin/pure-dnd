import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import BoardsItems from './components/BoardItems/BoardItems.jsx';

ReactDOM.render(
  <React.StrictMode>
    <BoardsItems/>
  </React.StrictMode>,
  document.getElementById('root')
);
